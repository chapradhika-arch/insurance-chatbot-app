/**
 * Angular environment file — PRODUCTION.
 *
 * IMPORTANT: This file is bundled into the JS that ships to the browser.
 * NEVER put API keys, tokens, or other secrets here.
 * Replace `apiBaseUrl` with your real backend URL at deploy time.
 */
export const environment = {
  production: true,
  apiBaseUrl: 'https://your-backend.example.com/api',
};
